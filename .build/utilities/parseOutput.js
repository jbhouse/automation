"use strict";
module.exports = {
    parseOutput: function (keyWord, input) { return parseOutput(keyWord, input); }
};
function parseOutput(keyWord, input) {
    console.log("input: " + input);
    console.log("keyWord: " + keyWord);
}
