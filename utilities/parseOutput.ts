module.exports = {
    parseOutput: (keyWord: string, input: string) => parseOutput(keyWord, input)
}

function parseOutput(keyWord: string, input: string) {
    console.log("input: " + input);
    console.log("keyWord: " + keyWord);
}